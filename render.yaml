services:
  - type: web
    name: smart-homework-planner-backend
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: JWT_EXPIRE_MINUTES
        value: "1440"
